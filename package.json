{
  "name": "edge-cms-root",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": ">=16.0.0"
  },
  "scripts": {
    "build": "turbo run build",
    "release": "turbo run release"
  },
  "workspaces": [
    "packages/*",
    "workers/*"
  ],
  "packageManager": "yarn@3.6.0",
  "devDependencies": {
    "turbo": "^1.10.2",
    "wrangler": "^3.1.0"
  }
}
